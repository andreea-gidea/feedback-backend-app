package com.endava.endavibe.common.util.enums;

public enum AlertType {
    AVG_TOO_LOW("Current average value is too low"),
    DECLINING_VALUE("Average value is declining");

    AlertType(String s) {
        /* EMPTY */
    }
}
